# ==========================================
# UDPxy Scanner — 环境变量配置
# ==========================================
# 复制此文件为 .env，按需修改配置
#
# Docker Compose 会自动读取 .env 文件中的变量，
# 因此配置好后只需运行: docker compose --profile caddy up -d

# --- Docker Hub 用户名（多架构镜像构建时使用） ---
# 构建多架构镜像前，先在 .env 中设置你的 Docker Hub 用户名
# Docker Hub 的镜像名格式: 你的用户名/镜像名:标签
# DOCKER_USERNAME=your-dockerhub-username

# --- 基础镜像 registry 镜像源（国内用户必设） ---
# 由于 buildx builder 不走 Docker Daemon 的镜像加速，
# 必须通过此参数指定国内镜像源才能拉取基础镜像。
# 可选值: docker.m.daocloud.io / docker.unsee.tech / docker.1panel.live
# BASE_REGISTRY=docker.m.daocloud.io

# --- 国内镜像仓库（可选，推送到国内镜像仓库替代 Docker Hub） ---
# 如果 Docker Hub 推送失败（国内网络问题），可改用阿里云容器镜像服务等
# 1. 在阿里云/腾讯云/华为云创建容器镜像仓库
# 2. 登录: docker login registry.cn-hangzhou.aliyuncs.com
# 3. 设置:
# DOCKER_REGISTRY=registry.cn-hangzhou.aliyuncs.com/你的命名空间

# --- 默认管理员密码（首次启动时初始化） ---
# PASSWORD=admin

# --- Caddy HTTPS 反向代理（需要域名） ---
# 启用方式: docker compose --profile caddy up -d
# 前提: 域名已 DNS 解析到本机公网 IP
#
# 必填: 你的域名
# DOMAIN=example.com
#
# 选填: Let's Encrypt 证书过期提醒邮箱
# EMAIL=admin@example.com
